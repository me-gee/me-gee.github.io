{
  "name": "hyeji-portfolio",
  "version": "1.0.0",
  "description": "HYEJI Portfolio with SCSS",
  "main": "index.html",
  "scripts": {
    "build": "sass assets/scss/style.scss assets/css/style.css --style compressed",
    "watch": "sass assets/scss/style.scss assets/css/style.css --watch",
    "dev": "sass assets/scss/style.scss assets/css/style.css --watch --style expanded",
    "deploy": "npm run build && git add . && git commit -m 'Update portfolio' && git push origin main"
  },
  "keywords": ["portfolio", "scss", "frontend"],
  "author": "HYEJI",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.69.0"
  }
} 